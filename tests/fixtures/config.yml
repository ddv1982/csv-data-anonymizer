columns:
  - name: email_address
    type: email
    options:
      preserveDomain: true
  - name: customer_id
    type: uuid
  - name: id
    type: numeric_id
    options:
      preserveDigitCount: true

output: output_anonymized.csv
deterministic: true
seed: "reproducible-seed-value"
